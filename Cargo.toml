[workspace]
resolver = "2"

members = [
  "native/rzjson",
  "native/host",
  "native/methods"
, "native/testcrate"]

# Always optimize; building and running the guest takes much longer without optimization.
[profile.dev]
opt-level = 3

[profile.release]
debug = 1
lto = true
