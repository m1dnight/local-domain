[package]
name = "zkvm"
version = "0.1.0"
authors = []
edition = "2021"

[lib]
name = "zkvm"
crate-type = ["cdylib"]

[dependencies]
rustler = "0.36.2"

risc0-zkvm = { version = "^2.1.0" }
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
serde = "1.0"
